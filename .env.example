# Copy to .env in the TARGET project (never commit real values).
NOTION_TOKEN=
BASTION_KEY_PATH=/Users/elliot18/.ssh/bastion.pem
# Google Workspace (Gmail + Calendar + Drive) — replaces the claude.ai Google connectors.
# Create an OAuth client in Google Cloud Console (Desktop app), enable Gmail/Calendar/Drive APIs.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# field-docs-check skill: folder that holds .env.local (AIRTABLE_WRITE_TOKEN, N8N_API_KEY, FILLOUT_API_KEY)
# and google-sheets-sa.json (the sheets-editor service-account key). Or export those variables directly.
FDC_DASH_DIR=/Users/elliot18/Projects/trajekt/trajekt/trajekt-revenue-dashboard
GOOGLE_SHEETS_SA_JSON=
