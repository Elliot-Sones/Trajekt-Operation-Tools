# Copy to .env in the TARGET project (never commit real values).
NOTION_TOKEN=
BASTION_KEY_PATH=/Users/elliot18/.ssh/trajekt-bastion.pem
# Google Workspace (Gmail + Calendar + Drive) — replaces the claude.ai Google connectors.
# Create an OAuth client in Google Cloud Console (Desktop app), enable Gmail/Calendar/Drive APIs.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
